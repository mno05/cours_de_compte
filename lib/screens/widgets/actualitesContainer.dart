import 'package:flutter/material.dart';

class ActualiteContainer extends StatelessWidget {
  const ActualiteContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
